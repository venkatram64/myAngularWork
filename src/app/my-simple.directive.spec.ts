import { MySimpleDirective } from './my-simple.directive';

describe('MySimpleDirective', () => {
  it('should create an instance', () => {
    const directive = new MySimpleDirective();
    expect(directive).toBeTruthy();
  });
});
